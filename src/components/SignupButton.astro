---
import {
  isFeatureActive,
  getFeatureUrl,
  getFeatureText,
} from "../config/siteConfig.ts";
import { useTranslations } from "../utils/useTranslations.ts";
const { t } = useTranslations(Astro.url);
---

{
  isFeatureActive("signupButton") && (
    <section class="text-center my-8">
      <a
        href={getFeatureUrl("signupButton")}
        target="_blank"
        rel="noopener noreferrer"
        class="
          inline-flex items-center gap-2
          bg-yellow-400 hover:bg-yellow-300 text-black font-bold
          px-8 py-4 rounded-full
          shadow-xl hover:shadow-2xl
          transform hover:scale-105 active:scale-95
          transition-all duration-300
          border-2 border-yellow-500 hover:border-yellow-300
          text-lg md:text-xl
          focus:outline-none focus:ring-4 focus:ring-yellow-400/50
        "
      >
        <span role="img" aria-label="påmelding">
          🏃‍♂️
        </span>
        {getFeatureText("signupButton") || t("signup.button")}
        <span role="img" aria-label="pil høyre">
          →
        </span>
      </a>
    </section>
  )
}
